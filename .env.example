# Copy to .env.local and fill in to enable live Claude analysis when
# running `vercel dev` locally. Without this, the app runs fine using
# mock data — see README.md "Enabling live AI analysis".
ANTHROPIC_API_KEY=
